# 예산현액 뷰어 (BudgetViewer) 개발일지 (Development Log)

본 문서는 **예산현액 뷰어 (BudgetViewer)** 프로젝트의 맥락과 기술적 핵심을 담은 개발일지입니다. 다른 개발자나 AI 에이전트가 이 프로젝트를 이어서 수정하더라도 시스템의 의도와 절대 유지되어야 할 핵심 구조를 한눈에 파악할 수 있도록 구체적으로 기술되었습니다.

---

## 1. 사용자 예산 활용의 흐름 (User Budget Flow)

사용자는 주로 지방자치단체 혹은 공공기관의 예산 집행 현황을 모니터링하기 위해 본 프로그램을 사용합니다. 전체적인 흐름은 다음과 같습니다.

1. **엑셀 파일 로드 (Excel Loading)**:
   - 사용자는 원본 예산현액 엑셀 파일을 업로드합니다.
   - 엑셀 파일은 다중 헤더(보통 2줄) 구조와 일상경비교부액을 포함하는 복잡한 계층형 테이블 구조를 가지고 있습니다.
   
2. **동적 열 분석 및 자동 매핑 (Dynamic Column Analysis & Mapping)**:
   - 파일 내에서 예산현액, 배부액, 교부액, 원인행위액, 지출액, 배부잔액, 교부잔액 등의 열을 헤더의 기호(예: `(D)`, `(I)`, `(J)` 등) 및 키워드로 자동 식별합니다.
   - 사용자 핵심 요구 사항에 따라 실질적인 **'잔액'**을 `교부액 - 지출액` 수식으로 자동 계산하여 내부 열로 생성합니다.

3. **분석 모드 및 필터 탐색 (Filtering & Exploration)**:
   - 사용자는 대과제(L1 - 세세부사업), 사업명(L2/L3), 예산코드를 필터 창을 통해 켜고 끄며 탐색할 수 있습니다.
   - '표시 항목' 버튼을 클릭해 보고 싶은 수치 컬럼만 선택하여 화면을 최적화할 수 있습니다.
   - '0원 행 숨김' 옵션을 활성화하여 지출액과 잔액이 모두 0원인 불필요한 계층 노드를 필터링하여 보고서를 깔끔하게 만듭니다.

4. **현재 화면 엑셀 내보내기 (Exporting)**:
   - 사용자가 필터를 적용하고 열 가시성을 편집한 상태 그대로의 구조와 값을 유지하여 새로운 엑셀 파일(`.xlsx`)로 내보낼 수 있습니다.

---

## 2. 주요 기능의 구현 의도 (Intended Implementation of Key Features)

### 2.1 절대 합계 (Absolute Totals) 방식의 계산 구조
- **의도**: 트리 뷰 필터링 조건에 따라 하위 노드들이 화면에서 보이지 않게 되더라도, 상위 부모 노드(L1, L2 등)에 표시되는 소계/합계는 **필터링 여부와 무관하게 언제나 원본 데이터의 절대적인 총합**을 유지해야 합니다.
- **방식**: 
  - 파일 로드 시점에 원본 데이터를 순회하며 실제 예산의 가장 말단에 위치한 로우(Leaf Node)들을 판별하고, 이들만 합산하여 `l1_group_totals`와 `l2_group_totals` 딕셔너리에 미리 저장합니다.
  - 트리 모델을 빌드할 때, 상위 계층 로우는 자식 노드의 값을 런타임에 다이내믹하게 합산하지 않고, 미리 계산된 이 딕셔너리에서 키를 조회하여 상수로 채워 넣습니다.
  - 필터링 시에는 하단에 **'∑ 소계 (현재 필터 결과)'**라는 별도의 동적 요약 행을 추가하여 현재 사용자가 보고 있는 데이터의 합계만 따로 집계해 줍니다.

### 2.2 자동 계층화 탐색 방식 (`_should_use_fourth_detail`)
- **의도**: 엑셀 데이터의 계층 구조는 조직/사업에 따라 3세부(L2) 또는 4세부(L3)까지 유동적입니다. 이를 수동으로 설정하기 번거로우므로 똑똑하게 자동 판단해야 합니다.
- **방식**:
  - `_should_use_fourth_detail` 함수는 특정 3세부 사업 하위에 원가비목이 비어 있는 상세 항목들이 유의미하게 존재하는지 비율을 계산합니다.
  - 해당 조건에 부합하면 자동으로 계층을 더 깊게(4세부 레벨) 펼쳐 보여주고, 그렇지 않으면 3세부 레벨로 압축하여 화면의 복잡도를 줄여 줍니다.

### 2.3 열 가시성 매핑 및 설정 영속성 (`QSettings`)
- **의도**: 사용자가 프로그램의 열 표시 선택(예: 잔액 노출, 배부액 숨김 등)을 편집하고 프로그램을 재시작했을 때, 그 상태가 똑같이 유지되도록 합니다.
- **방식**:
  - 사용자에게 보이는 팝업 창 체크박스 텍스트에는 단순하고 직관적인 이름(예: `잔액`)을 노출합니다.
  - 실제 트리 헤더에는 정보성 수식(`잔액\n(교부액-지출액)`)을 적용해야 하므로, 설정 로드/저장 시점에 이 두 이름 간의 상호 매핑 로직(`_on_col_visibility_changed`)을 제공하여 데이터 일치성을 지킵니다.

---

## 3. 추후 수정에서 흔들리면 안 될 핵심 구조 (Architectural Invariants)

이 프로젝트를 수정하거나 새로운 요구사항을 얹을 때, **아래 구조는 절대 훼손되어서는 안 됩니다.**

### 3.1 중복 합산 방지 장치: `_is_agg_leaf` 플래그
- **이유**: 엑셀 원본 파일 자체에 이미 '소계', '합계' 등의 행이 섞여 들어오거나, 부모 노드의 텍스트가 중복되어 존재할 수 있습니다. 
- **원칙**: 데이터를 로드하고 계층을 분석할 때 반드시 `_is_agg_leaf` 플래그를 올바르게 계산하여 마킹해야 합니다. 합계를 계산할 때는 오직 이 플래그가 `True`인 행만 사용해야 중복 합산(Discrepancy) 오류가 발생하지 않습니다.
- **경고**: 분류 복구를 위해 행을 함부로 삭제해서는 안 되며, 계층 판정 함수(`_parse_level`, `_is_hierarchical`)를 수정할 경우 반드시 `_is_agg_leaf` 계산 결과를 검증해야 합니다.

### 3.2 상위 노드의 사전 계산된 합계 바인딩 (`l1_totals`, `l2_totals`)
- **이유**: 사용자는 필터링 조건에 따라 화면의 일부분만 볼 때도 전체 예산 대비 해당 사업의 비중을 인지하고 싶어 합니다.
- **원칙**: 트리 모델(`tree_model.py`)에서 `l1_item` 및 `biz_item` 행을 그릴 때 적용되는 금액 데이터는 절대 `df[...].sum()` 등의 방식으로 동적 합산해서는 안 되며, `self.l1_totals.get(...)` 및 `self.l2_totals.get(...)`를 통해 바인딩해야 합니다.

### 3.3 '잔액'의 수식 명칭 매핑 규칙
- **이유**: UI 컨트롤러(`main.py`)의 체크박스 상태와 모델(`tree_model.py`)의 헤더 텍스트 간의 명칭 불일치로 인한 오작동을 막아야 합니다.
- **원칙**: 내부적으로 잔액 데이터의 키는 `잔액\n(교부액-지출액)`(개행 포함)으로 동작하고, UI 팝업에서는 `잔액`으로 관리됩니다. 이 둘을 매핑하는 변환 코드(`name == "잔액" => name = "잔액\n(교부액-지출액)"`)를 누락하면 설정 저장 시 예외가 발생하거나 설정이 저장되지 않는 버그가 생깁니다.

---

## 4. 주요 개발 흐름 및 히스토리 (Development History)

*   **v1.5.0**:
    - 최초 구현. 엑셀의 계층 파싱 및 화면 노출 기본형 완성.
*   **v1.6.0 (안정화 및 자동화)**:
    - **버그 수정**: 필터링이나 요약 행 탐색 과정에서 전체 금액 합계가 비정상적으로 부풀려지던 중복 합산 오류 수정 (`_is_agg_leaf` 기준 프리-어그리게이션 도입).
    - **버전 기록기 도입**: 코드 백업 및 버전 관리를 자동화하는 `version_manager.py` 추가.
    - **GitHub Actions 연동**: 신규 버전 태그(`v*`)가 리포지토리에 푸시되면 자동으로 가상 환경에서 PyInstaller를 실행하여 Windows용 릴리즈 빌드(.exe)를 완성하고, 이를 GitHub Releases에 릴리즈 노트와 함께 자동 업로드해 주는 CI/CD 빌드 파이프라인(`release.yml`) 구축.
*   **v1.7.0 (사용자 경험 개선)**:
    - **헤더 시인성**: 트리 뷰 헤더의 '잔액' 열 밑에 `(교부액-지출액)` 수식 표시 추가 및 헤더 최소 높이 조정(44px).
    - **초기 노출 설정 변경**: 최초 실행 시 사용 빈도가 적은 '배부액'을 제외한 Core 5개 열(예산현액, 교부액, 원인행위액, 지출액, 잔액)만 기본 노출하도록 초기화 코드 수정.
    - **표시 항목 팝업 UI 수정**: 불필요한 스페이서(addStretch)를 제거하고 높이 계산식을 조율하여 하단의 지저분한 빈 공간(Gap) 제거.
