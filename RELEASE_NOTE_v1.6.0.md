## 🚀 v1.6.0: 예산 합계 및 세부 항목 집계 오류 수정

이번 버전에서는 예산 총계가 중복 계산되던 심각한 오류를 해결하고, 분석 모드에 따른 중간 합계 표시 기능을 대폭 강화했습니다.

### ✅ 주요 변경 사항

*   **예산 총계 산출 로직 전면 개편**: 엑셀 시트 내의 '소계', '합계' 행과 예산코드 행이 데이터 행으로 오인되어 중복 합산되던 문제를 해결했습니다. 이제 프로그램의 총계가 엑셀의 '소계' 값과 1원 단위까지 정확히 일치합니다.
*   **중간 항목(L2, L3) 합계 표시**: 3세부/4세부 등 분석 모드를 전환할 때 중간 항목(○)의 합계가 0으로 나오던 현상을 수정하여, 어떤 모드에서도 모든 계층의 사업 금액을 확인할 수 있습니다.
*   **자체 버전 관리 및 백업 시스템 도입**: 프로젝트 루트에 `version_manager.py`를 추가하여, 업데이트 시마다 자동으로 이전 버전을 `backups` 폴더에 스냅샷으로 저장합니다.

### 🛠 기술적 개선

*   `loader.py`: 계층 구조 분석(`_is_agg_leaf`) 및 전역 집계 로직 개선
*   `CHANGELOG.md`: 프로젝트 변경 이력 관리 시작
*   실행 파일 빌드 최적화 (PyInstaller v6.19.0 적용)

### 📥 설치 및 실행 방법

1.  아래 **Assets** 영역에서 **`BudgetViewer(v1.6.0).exe`** 파일을 다운로드합니다.
2.  별도의 설치나 파이썬 환경 없이 바로 실행하여 사용하실 수 있습니다.
